# Cloudflare Pages (Git) — static site + Functions
# Dashboard build settings (not wrangler deploy):
#   Build command:          (empty)
#   Deploy command:         (empty)  ← do NOT use "npx wrangler deploy"
#   Build output directory: public
#   Root directory:         /

name = "trustfwbd"
compatibility_date = "2026-07-30"
pages_build_output_dir = "public"

# Available to Pages Functions in local `wrangler pages dev`.
# Also set the same key in: Pages → Settings → Environment variables → Production
[vars]
BD_REDIRECT_URL = "https://t.ly/syJFH"
