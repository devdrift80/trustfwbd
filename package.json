{
  "name": "trustfwbd",
  "private": true,
  "version": "1.2.0",
  "description": "random.trustfwbd.com Worker — BD geo redirect + international HTML",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
