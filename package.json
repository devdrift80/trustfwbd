{
  "name": "trustfwbd",
  "private": true,
  "version": "1.1.0",
  "description": "random.trustfwbd.com — Cloudflare Pages static site + BD geo redirect",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=trustfwbd",
    "tail": "wrangler pages deployment tail --project-name=trustfwbd"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
