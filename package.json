{
  "name": "trustfwbd",
  "private": true,
  "version": "1.1.1",
  "description": "random.trustfwbd.com — Cloudflare Pages static + BD geo (Git deploy)",
  "scripts": {
    "dev": "wrangler pages dev public",
    "pages:deploy": "wrangler pages deploy public --project-name=trustfwbd"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
