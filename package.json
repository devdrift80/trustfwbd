{
  "name": "trustfwbd",
  "private": true,
  "version": "1.1.2",
  "description": "random.trustfwbd.com — Cloudflare Pages static + BD geo",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "echo \"Static site — output is public/\"",
    "deploy": "wrangler pages deploy public --project-name=trustfwbd"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
